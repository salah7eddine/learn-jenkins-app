pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'echo "Hello World from Jenkins !!"'
                sh 'whoami'
            }
        }
    }
}
